/**
 * OpenCode provider — UI adapter extraction (stub).
 * Phase 4: implement when needed.
 */

import type { CanonicalToolData } from "@t3tools/contracts";

export function extractOpenCodeToolData(_payload: unknown): CanonicalToolData | null {
  return null;
}
