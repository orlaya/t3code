// This file exists purely to test that Write tool diffs
// render correctly in the inline edit diff timeline.
// Delete me whenever.

export function greet(name: string): string {
  return `Hello, ${name}!`;
}

export const ITEMS = ["apple", "banana", "cherry"] as const;
